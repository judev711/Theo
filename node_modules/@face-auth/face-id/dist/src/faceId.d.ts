import { Either, IdentifyResponse, RegisterResponse, UnregisterResponse } from "./response";
export declare class FaceId {
    private readonly _domain;
    private _auth;
    private _authPromise;
    constructor(domain: string, clientToken: string);
    private authorize;
    register(faceId: string, image: string, imageType: string): Promise<Either<RegisterResponse>>;
    identify(image: string, imageType: string): Promise<Either<IdentifyResponse>>;
    unregister(faceId: string): Promise<Either<UnregisterResponse>>;
    private handleResponse;
    private handleError;
    private unauthorized;
    private isAuth;
}
